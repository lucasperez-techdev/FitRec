import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Profile Settings - FitRec",
  description: "Edit your FitRec profile settings",
};

export default function ProfilePage() {
  return (
    <div>
      <h1>Profile</h1>
    </div>
  );
} 